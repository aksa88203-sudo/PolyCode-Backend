# Memory Management

Dynamic memory allocation using malloc/free.

## Example
```c
int *ptr = (int*) malloc(5*sizeof(int));
free(ptr);
Practice

Allocate memory for 10 integers, initialize, print, free.
