# Setup

## Objective
Get your C environment running and compile your first program.

## Checklist
- Confirm compiler: `gcc --version`
- Compile and run one file

## Practice
```bash
cd "setup"
gcc "first_program.c" -o first_program
./first_program
```
