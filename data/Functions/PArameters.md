# Function Parameters

Functions can take inputs via parameters.

## Example
```cpp
void greet(string name) { cout << "Hello " << name << endl; }
Practice

Create a function multiply(a,b) that prints their product.
