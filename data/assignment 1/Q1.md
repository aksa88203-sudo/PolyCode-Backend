# Q1 Menu Management System - Comprehensive Line-by-Line Explanation

## Header File: menumanagementsystem.h

### Header Guards and Includes (Lines 1-7)
```cpp
#ifndef MENUMANAGEMENTSYSTEM_H
#define MENUMANAGEMENTSYSTEM_H

#include <iostream>
#include <fstream>
#include <string>
using namespace std;
```
- **Line 1**: `#ifndef` - "if not defined" - Prevents multiple inclusion of this header file
- **Line 2**: `#define` - Defines the macro to prevent re-inclusion
- **Line 4-6**: Include standard library headers for I/O operations, file handling, and string manipulation
- **Line 7**: `using namespace std` - Allows direct use of standard library components without `std::` prefix

### Constants Declaration (Lines 9-11)
```cpp
const int MAX_ROWS = 100;
const int MAX_COLS = 7;
const int MAX_LEN = 200;
```
- **Line 9**: `MAX_ROWS` - Maximum number of menu items that can be processed
- **Line 10**: `MAX_COLS` - Number of fields per menu item (Category, Cuisine, Item Name, Price, Restaurant, Cook Name, Calories)
- **Line 11**: `MAX_LEN` - Maximum length for string processing (design constraint)

### String Manipulation Function Declarations (Lines 13-21)
```cpp
// String manipulation functions
int stringLength(const string &s);
int stringFind(const string &s, char c);
int stringFind(const string &s, const string &substr);
string stringSubstr(const string &s, int pos, int len);
int stringToInt(const string &s);
bool customGetline(istream &input, string &line);
string trim(const string &s);
int splitLine(const string &line, string tokens[], int maxTokens);
```
- **Line 13**: Comment section header for string functions
- **Line 14**: `stringLength` - Calculate string length without using built-in methods
- **Line 15**: `stringFind` (char version) - Find character position in string
- **Line 16**: `stringFind` (string version) - Find substring position in string
- **Line 17**: `stringSubstr` - Extract substring from specified position
- **Line 18**: `stringToInt` - Convert string to integer value
- **Line 19**: `customGetline` - Read line from input stream without built-in getline
- **Line 20**: `trim` - Remove leading and trailing whitespace
- **Line 21**: `splitLine` - Split comma-separated line into tokens

### Menu Processing Function Declarations (Lines 23-28)
```cpp
// Menu processing functions
int categoryOrder(const string &cat);
string baseCuisine(const string &cuisine);
bool compareLess(string **arr, int a, int b);
void sortMenu(string **dynArr, int rows);
void displayItem(string **dynArr, int idx);
```
- **Line 23**: Comment section header for menu processing functions
- **Line 24**: `categoryOrder` - Assign numeric priority to menu categories
- **Line 25**: `baseCuisine` - Extract base cuisine from hyphenated names
- **Line 26**: `compareLess` - Multi-field comparison for sorting
- **Line 27**: `sortMenu` - Sort menu items using bubble sort
- **Line 28**: `displayItem` - Display formatted menu item information

### Header Guard Closing (Line 30)
```cpp
#endif // MENUMANAGEMENTSYSTEM_H
```
- **Line 30**: `#endif` - Closes header guard, preventing multiple inclusions

---

## Implementation File: 25L-3045 (Q1).cpp

### Header Include (Line 1)
```cpp
#include "menumanagementsystem.h"
```
- **Line 1**: Includes the header file with all function declarations and constants

### Main Function (Lines 3-155)
```cpp
int main()
{
    string staticArr[MAX_ROWS][MAX_COLS];
    int rowCount = 0;

    ifstream fin;
    string pathsToTry[] = {
        "Q1.txt",
        "solution/Q1.txt",
        "Assignment 1/solution/Q1.txt",
        "Assignments/Assignment 1/solution/Q1.txt",
        "C:/Software Engineering/Semester 2/Object Oriented Programming/Assignments/Assignment 1/solution/Q1.txt"};
    for (int p = 0; p < 5; p++)
    {
        fin.open(pathsToTry[p]);
        if (fin.is_open())
        {
            cout << "Opened file: " << pathsToTry[p] << endl;
            break;
        }
    }
    if (!fin.is_open())
    {
        cout << "Error: Could not open Q1.txt in any expected location." << endl;
        cout << "Edit the absolute path in the code to match your system." << endl;
        return 1;
    }

    string line;
    while (customGetline(fin, line) && rowCount < MAX_ROWS)
    {
        line = trim(line);
        if (line.empty())
            continue;
        string tokens[MAX_COLS];
        int count = splitLine(line, tokens, MAX_COLS);
        for (int j = 0; j < MAX_COLS; j++)
        {
            staticArr[rowCount][j] = (j < count) ? tokens[j] : "";
        }
        rowCount++;
    }
    fin.close();

    cout << "Read " << rowCount << " records from file." << endl;

    int DCOLS = MAX_COLS + 1;
    string **dynArr = new string *[rowCount];
    for (int i = 0; i < rowCount; i++)
    {
        dynArr[i] = new string[DCOLS];
        for (int j = 0; j < MAX_COLS; j++)
        {
            dynArr[i][j] = staticArr[i][j];
        }
        dynArr[i][MAX_COLS] = "\0";
    }

    sortMenu(dynArr, rowCount);

    cout << "\n========== SORTED MENU ==========\n"
         << endl;
    cout << "Sorted by: Cuisine Type -> Restaurant Name -> Category -> Price\n"
         << endl;
    for (int i = 0; i < rowCount; i++)
    {
        cout << "Item #" << (i + 1) << ":" << endl;
        displayItem(dynArr, i);
    }

    cout << "\n========== SEARCH BY CUISINE TYPE ==========\n"
         << endl;
    cout << "Enter cuisine type to search (e.g., Mexican, Experimental, Fusion): ";
    string searchCuisine;
    customGetline(cin, searchCuisine);
    searchCuisine = trim(searchCuisine);

    cout << "\nResults for cuisine containing \"" << searchCuisine << "\":\n"
         << endl;
    bool found = false;
    for (int i = 0; i < rowCount; i++)
    {
        string cuisine = dynArr[i][1];
        if (cuisine == searchCuisine ||
            (stringLength(cuisine) > stringLength(searchCuisine) && stringSubstr(cuisine, 0, stringLength(searchCuisine)) == searchCuisine && cuisine[stringLength(searchCuisine)] == '-') ||
            (stringLength(cuisine) > stringLength(searchCuisine) && stringFind(cuisine, searchCuisine) == 0))
        {
            displayItem(dynArr, i);
            found = true;
        }
    }
    if (!found)
    {
        cout << "No items found for cuisine: " << searchCuisine << endl;
    }

    ofstream fout("C:/Software Engineering/Semester 2/Object Oriented Programming/Assignments/Assignment 1/solution/sorted_menu.txt");
    if (!fout)
    {
        cout << "\nWarning: Could not create sorted_menu.txt" << endl;
    }
    else
    {
        fout << "========== ORGANIZED MENU ==========\n\n";
        string lastCategory = "";
        for (int i = 0; i < rowCount; i++)
        {
            if (dynArr[i][0] != lastCategory)
            {
                lastCategory = dynArr[i][0];
                fout << "\n*" << lastCategory << "*\n";
            }
            fout << "  " << dynArr[i][2]
                 << "  |  Price: " << dynArr[i][3]
                 << "  |  Restaurant: " << dynArr[i][4]
                 << "  |  Cook: " << (dynArr[i][5].empty() ? "[missing]" : dynArr[i][5])
                 << "  |  Calories: " << (dynArr[i][6].empty() ? "[missing]" : dynArr[i][6])
                 << "\n";
        }
        fout.close();
        cout << "\nSorted menu exported to sorted_menu.txt" << endl;
        cout << "Both files (menu_data.txt and sorted_menu.txt) are saved in the SAME folder as your .exe" << endl;
    }

    for (int i = 0; i < rowCount; i++)
    {
        delete[] dynArr[i];
    }
    delete[] dynArr;

    return 0;
}
```
- **Line 3**: Main function entry point
- **Line 5**: Declare static 2D array for initial file storage
- **Line 6**: Counter for actual number of rows read
- **Line 8**: Input file stream object
- **Line 9-15**: Array of possible file paths to try
- **Line 16-22**: Loop through paths, attempt to open file
- **Line 23-28**: Error handling if no file can be opened
- **Line 30**: String variable for reading each line
- **Line 31-38**: Read file line by line, process each record
- **Line 32**: Trim whitespace from line
- **Line 33-34**: Skip empty lines
- **Line 35**: Array to hold split tokens
- **Line 36**: Split line by commas
- **Line 37-40**: Copy tokens to static array, fill empty fields with ""
- **Line 41**: Increment row counter
- **Line 43**: Close input file
- **Line 45**: Display read count
- **Line 47**: Dynamic columns count (data + null terminator)
- **Line 48**: Allocate dynamic array of string pointers
- **Line 49-55**: Allocate each row and copy data from static array
- **Line 56**: Add null terminator to each row
- **Line 59**: Sort the dynamic array
- **Line 61-66**: Display sorted menu header
- **Line 67-71**: Loop through and display all sorted items
- **Line 73-77**: Search functionality header
- **Line 78**: Prompt user for cuisine search
- **Line 79-80**: Get and trim user input
- **Line 81-84**: Search results header
- **Line 85**: Flag to track if any items found
- **Line 86-96**: Search loop with complex matching logic
- **Line 87**: Get cuisine from current row
- **Line 88-92**: Multiple search conditions (exact match, prefix with hyphen, substring)
- **Line 93-95**: Display item if match found
- **Line 97-100**: Handle no results case
- **Line 102**: Output file stream for sorted menu
- **Line 103-107**: Handle file creation error
- **Line 108-126**: Write sorted menu to file with category grouping
- **Line 109**: File header
- **Line 110**: Track last category for grouping
- **Line 111-125**: Loop through items, group by category
- **Line 112-115**: New category header when category changes
- **Line 116-124**: Write item details with missing field handling
- **Line 125**: Close output file
- **Line 126-128**: Success messages
- **Line 130-134**: Memory cleanup - delete each row
- **Line 135**: Delete main array pointer
- **Line 137**: Return success

### String Manipulation Functions (Lines 159-313)

#### stringLength Function (Lines 159-175)
```cpp
int stringLength(const string &s)
{
    int len = 0;
    while (s[len] != '\0')
    {
        len++;
    }
    return len;
}
```
- **Line 159**: Function definition with const string reference parameter
- **Line 161**: Initialize length counter to zero
- **Line 162-165**: Loop through string until null terminator found
- **Line 163**: Increment counter for each character
- **Line 167**: Return final length count

#### stringFind Function - Character Version (Lines 167-185)
```cpp
int stringFind(const string &s, char c)
{
    for (int i = 0; i < stringLength(s); i++)
    {
        if (s[i] == c)
            return i;
    }
    return -1;
}
```
- **Line 167**: Function to find character position
- **Line 169**: Loop through string using custom stringLength
- **Line 170-172**: Check each character, return position if found
- **Line 174**: Return -1 if character not found

#### stringFind Function - Substring Version (Lines 177-209)
```cpp
int stringFind(const string &s, const string &substr)
{
    int sLen = stringLength(s);
    int subLen = stringLength(substr);
    
    if (subLen == 0) return 0;
    if (subLen > sLen) return -1;
    
    for (int i = 0; i <= sLen - subLen; i++)
    {
        bool match = true;
        for (int j = 0; j < subLen; j++)
        {
            if (s[i + j] != substr[j])
            {
                match = false;
                break;
            }
        }
        if (match) return i;
    }
    return -1;
}
```
- **Line 177**: Function to find substring position
- **Line 179-180**: Get lengths of both strings
- **Line 182-183**: Handle edge cases (empty substring, substring longer than string)
- **Line 185**: Outer loop for possible starting positions
- **Line 186-196**: Inner loop to check character-by-character match
- **Line 187**: Assume match is true
- **Line 188-195**: Check each character, break if mismatch
- **Line 197**: Return position if full match found
- **Line 199**: Return -1 if no match found

#### stringSubstr Function (Lines 201-226)
```cpp
string stringSubstr(const string &s, int startposition, int len)
{
    string result = "";
    int sLen = stringLength(s);
    
    if (startposition < 0 || startposition >= sLen) return result;
    
    int endposition = startposition + len;
    if (endposition > sLen) endposition = sLen;
    
    for (int i = startposition; i < endposition; i++)
    {
        result += s[i];
    }
    return result;
}
```
- **Line 201**: Function to extract substring
- **Line 203**: Initialize result string
- **Line 204**: Get source string length
- **Line 206-207**: Boundary check for invalid start position
- **Line 209**: Calculate theoretical end position
- **Line 210**: Clamp end position to string length
- **Line 212-216**: Loop through specified range, build result
- **Line 213**: Append each character to result
- **Line 218**: Return constructed substring

#### stringToInt Function (Lines 218-261)
```cpp
int stringToInt(const string &s)
{
    int result = 0;
    int i = 0;
    int len = stringLength(s);
    
    while (i < len && (s[i] == ' ' || s[i] == '\t'))
        i++;
    
    bool negative = false;
    if (i < len && s[i] == '-')
    {
        negative = true;
        i++;
    }
    else if (i < len && s[i] == '+')
    {
        i++;
    }
    
    for (; i < len; i++)
    {
        if (s[i] >= '0' && s[i] <= '9')
        {
            result = result * 10 + (s[i] - '0');
        }
        else
        {
            break;
        }
    }
    
    return negative ? -result : result;
}
```
- **Line 218**: Function to convert string to integer
- **Line 220-222**: Initialize variables
- **Line 224-225**: Skip leading whitespace
- **Line 227**: Initialize negative flag
- **Line 228-232**: Handle negative sign
- **Line 233-237**: Handle positive sign
- **Line 239-249**: Main digit conversion loop
- **Line 240**: Check if character is digit
- **Line 241**: Convert ASCII digit to numeric value and accumulate
- **Line 242-246**: Break on first non-digit character
- **Line 251**: Apply sign and return result

#### customGetline Function (Lines 253-285)
```cpp
bool customGetline(istream &input, string &line)
{
    line = "";
    char c;
    
    while (input.get(c))
    {
        if (c == '\n')
        {
            return true;
        }
        else if (c == '\r')
        {
            continue;
        }
        else
        {
            line += c;
        }
    }
    
    return !line.empty();
}
```
- **Line 253**: Function to read line from input stream
- **Line 255**: Initialize line to empty
- **Line 256**: Character buffer
- **Line 258**: Loop while characters can be read
- **Line 259-263**: Handle newline character (end of line)
- **Line 264-267**: Handle carriage return (Windows line endings)
- **Line 268-271**: Append regular characters
- **Line 273**: Return true if line was read, false if empty

#### trim Function (Lines 277-303)
```cpp
string trim(const string &s)
{
    int start = 0, end = stringLength(s) - 1;
    while (start <= end && (s[start] == ' ' || s[start] == '\t' || s[start] == '\r' || s[start] == '\n'))
    {
        start++;
    }
    while (end >= start && (s[end] == ' ' || s[end] == '\t' || s[end] == '\r' || s[end] == '\n'))
    {
        end--;
    }
    if (start > end)
    {
        return "";
    }
    return stringSubstr(s, start, end - start + 1);
}
```
- **Line 277**: Function to remove leading/trailing whitespace
- **Line 279**: Initialize start and end positions
- **Line 280-284**: Move start position forward past leading whitespace
- **Line 285-289**: Move end position backward past trailing whitespace
- **Line 290-292**: Handle case where string is all whitespace
- **Line 294**: Return trimmed substring using custom function

#### splitLine Function (Lines 295-323)
```cpp
int splitLine(const string &line, string tokens[], int maxTokens)
{
    int count = 0;
    string current = "";
    for (int i = 0; i < stringLength(line); i++)
    {
        if (line[i] == ',' && count < maxTokens - 1)
        {
            tokens[count++] = trim(current);
            current = "";
        }
        else
        {
            current += line[i];
        }
    }
    tokens[count++] = trim(current);
    return count;
}
```
- **Line 295**: Function to split comma-separated line
- **Line 297**: Initialize token counter
- **Line 298**: Current token accumulator
- **Line 299**: Loop through line characters
- **Line 300-304**: Handle comma delimiter
- **Line 301**: Add trimmed current token to array
- **Line 302**: Reset current token
- **Line 305-307**: Add character to current token
- **Line 309**: Add final token after loop
- **Line 310**: Return total token count

### Menu Processing Functions (Lines 315-387)

#### categoryOrder Function (Lines 315-334)
```cpp
int categoryOrder(const string &cat)
{
    if (cat == "Appetizer")
        return 0;
    if (cat == "Main Course")
        return 1;
    if (cat == "Dessert")
        return 2;
    return 3;
}
```
- **Line 315**: Function to assign sort priority to categories
- **Line 317-322**: Return numeric priority for known categories
- **Line 323**: Return default priority for unknown categories

#### baseCuisine Function (Lines 326-342)
```cpp
string baseCuisine(const string &cuisine)
{
    int pos = stringFind(cuisine, '-');
    if (pos != -1)
        return stringSubstr(cuisine, 0, pos);
    return cuisine;
}
```
- **Line 326**: Function to extract base cuisine from hyphenated names
- **Line 328**: Find hyphen position using custom function
- **Line 329-331**: Return substring before hyphen if found
- **Line 332**: Return original string if no hyphen

#### compareLess Function (Lines 334-361)
```cpp
bool compareLess(string **arr, int a, int b)
{
    string ca = baseCuisine(arr[a][1]);
    string cb = baseCuisine(arr[b][1]);
    if (ca != cb)
        return ca < cb;
    if (arr[a][4] != arr[b][4])
        return arr[a][4] < arr[b][4];
    int oa = categoryOrder(arr[a][0]);
    int ob = categoryOrder(arr[b][0]);
    if (oa != ob)
        return oa < ob;
    int pa = arr[a][3].empty() ? 0 : stringToInt(arr[a][3]);
    int pb = arr[b][3].empty() ? 0 : stringToInt(arr[b][3]);
    return pa < pb;
}
```
- **Line 334**: Multi-field comparison function for sorting
- **Line 336-337**: Get base cuisines for comparison
- **Line 338-339**: First priority: base cuisine alphabetical
- **Line 340-341**: Second priority: restaurant name alphabetical
- **Line 342-343**: Get category orders
- **Line 344-345**: Third priority: category order
- **Line 346-347**: Get prices (convert to int, default to 0 if empty)
- **Line 348**: Fourth priority: price numeric comparison

#### sortMenu Function (Lines 351-375)
```cpp
void sortMenu(string **dynArr, int rows)
{
    for (int i = 0; i < rows - 1; i++)
    {
        for (int j = 0; j < rows - i - 1; j++)
        {
            if (!compareLess(dynArr, j, j + 1))
            {
                string *temp = dynArr[j];
                dynArr[j] = dynArr[j + 1];
                dynArr[j + 1] = temp;
            }
        }
    }
}
```
- **Line 351**: Bubble sort implementation for menu items
- **Line 353-354**: Outer loop for sort passes
- **Line 355**: Inner loop for adjacent comparisons
- **Line 356-360**: Swap if elements are out of order
- **Line 357**: Use compareLess to determine order
- **Line 358-360**: Pointer swap for efficiency

#### displayItem Function (Lines 367-387)
```cpp
void displayItem(string **dynArr, int idx)
{
    cout << "  Category     : " << dynArr[idx][0] << endl;
    cout << "  Cuisine      : " << dynArr[idx][1] << endl;
    cout << "  Item Name    : " << dynArr[idx][2] << endl;
    cout << "  Price        : " << dynArr[idx][3] << endl;
    cout << "  Restaurant   : " << dynArr[idx][4] << endl;
    cout << "  Cook Name    : " << (dynArr[idx][5].empty() ? "[missing]" : dynArr[idx][5]) << endl;
    cout << "  Calories     : " << (dynArr[idx][6].empty() ? "[missing]" : dynArr[idx][6]) << endl;
    cout << "  ----------------------------------------" << endl;
}
```
- **Line 367**: Function to display formatted menu item
- **Line 368-374**: Display all 7 fields with labels
- **Line 375**: Use ternary operator to show "[missing]" for empty cook names
- **Line 376**: Use ternary operator to show "[missing]" for empty calories
- **Line 377**: Display separator line

## Design Patterns and Concepts Used

### 1. **Custom String Implementation**
- All standard string functions replaced with custom implementations
- Demonstrates fundamental character-by-character processing
- Shows understanding of null-terminated strings

### 2. **Multi-Level Sorting**
- Complex comparison function with 4 sorting priorities
- Bubble sort algorithm with pointer swapping
- Efficient memory management during sorting

### 3. **File I/O Operations**
- Multiple file path attempts for cross-platform compatibility
- Custom line reading for different line ending formats
- Output file generation with formatted content

### 4. **Memory Management**
- Static array for initial file storage
- Dynamic 2D array for processing
- Proper cleanup with delete[] operations

### 5. **Data Processing Pipeline**
- File reading → Line parsing → Data storage → Sorting → Display → Search → Export
- Each step uses custom functions for maximum control

### 6. **Error Handling**
- File opening failure detection
- Boundary checking in string operations
- Empty field handling in display functions

### 7. **Modular Design**
- Clear separation between string manipulation and menu processing
- Reusable utility functions
- Header file organization for clean interfaces

This implementation demonstrates comprehensive C++ programming skills including custom algorithm implementation, memory management, file processing, and modular design principles.
