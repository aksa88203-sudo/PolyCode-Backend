# Q4 Thesaurus-Based Writing Assistant - Comprehensive Line-by-Line Explanation

## Header File: ThesaurusBasedWritingAssistant.h

### Header Guards and Includes (Lines 1-6)
```cpp
#ifndef THESAURUS_BASED_WRITING_ASSISTANT_H
#define THESAURUS_BASED_WRITING_ASSISTANT_H

#include <iostream>
#include <string>
using namespace std;
```
- **Line 1**: `#ifndef` - "if not defined" - Prevents multiple inclusion of this header file
- **Line 2**: `#define` - Defines the macro to prevent re-inclusion
- **Line 4**: Include standard library header for I/O operations
- **Line 5**: Include string library for string manipulation
- **Line 6**: `using namespace std` - Allows direct use of standard library components without `std::` prefix

### Function Declarations (Lines 8-11)
```cpp
int countGroups(char *ws);
int countWords(const string &group);
string **CreateDynamicArray(char *ws, int &numGroups, int *&rowSizes);
```
- **Line 8**: `countGroups` - Counts groups separated by '#' delimiter
- **Line 9**: `countWords` - Counts words in a string group
- **Line 10**: `CreateDynamicArray` - Creates dynamic 2D array from thesaurus string

### Header Guard Closing (Line 12)
```cpp
#endif // THESAURUS_BASED_WRITING_ASSISTANT_H
```
- **Line 12**: `#endif` - Closes header guard, preventing multiple inclusions

---

## Implementation File: 25L-3045 (Q4).cpp

### Header Include (Line 1)
```cpp
#include "ThesaurusBasedWritingAssistant.h"
```
- **Line 1**: Includes the header file with all function declarations

### countGroups Function (Lines 3-16)
```cpp
int countGroups(char *ws)
{
    int count = 1;
    for (int i = 0; ws[i] != '\0'; i++)
    {
        if (ws[i] == '#')
        {
            count++;
        }
    }
    return count;
}
```
- **Line 3**: Function definition with C-style string parameter
- **Line 5**: Initialize count to 1 (at least one group exists)
- **Line 6**: Loop through string until null terminator
- **Line 7-11**: Check each character, increment count for '#' delimiter
- **Line 13**: Return total number of groups
- **Purpose**: Determines how many synonym groups exist in thesaurus string

### countWords Function (Lines 18-35)
```cpp
int countWords(const string &group)
{
    int count = 0;
    bool inWord = false;
    for (int i = 0; i < (int)group.size(); i++)
    {
        if (group[i] != ' ' && !inWord)
        {
            inWord = true;
            count++;
        }
        else if (group[i] == ' ')
        {
            inWord = false;
        }
    }
    return count;
}
```
- **Line 18**: Function definition with string reference parameter
- **Line 20**: Initialize word count to zero
- **Line 21**: Initialize flag to track if currently inside a word
- **Line 22**: Loop through all characters in the group
- **Line 23-27**: Handle word start - non-space character when not in word
- **Line 24**: Set inWord flag to true
- **Line 25**: Increment word count
- **Line 28-31**: Handle word end - space character
- **Line 29**: Reset inWord flag to false
- **Line 33**: Return total word count
- **Purpose**: Counts individual words separated by spaces in a group

### CreateDynamicArray Function (Lines 37-90)
```cpp
string **CreateDynamicArray(char *ws, int &numGroups, int *&rowSizes)
{
    numGroups = countGroups(ws);
    rowSizes = new int[numGroups];

    string *groups = new string[numGroups];
    int gi = 0;
    string current = "";
    for (int i = 0; ws[i] != '\0'; i++)
    {
        if (ws[i] == '#')
        {
            groups[gi++] = current;
            current = "";
        }
        else
        {
            current += ws[i];
        }
    }
    groups[gi] = current;

    string **arr = new string *[numGroups];
    for (int i = 0; i < numGroups; i++)
    {
        rowSizes[i] = countWords(groups[i]);
        arr[i] = new string[rowSizes[i]];

        int wi = 0;
        string word = "";
        for (int j = 0; j < (int)groups[i].size(); j++)
        {
            if (groups[i][j] == ' ')
            {
                if (!word.empty())
                {
                    arr[i][wi++] = word;
                    word = "";
                }
            }
            else
            {
                word += groups[i][j];
            }
        }
        if (!word.empty())
        {
            arr[i][wi] = word;
        }
    }

    delete[] groups;
    return arr;
}
```
- **Line 37**: Function signature with reference parameters for output
- **Line 39**: Get number of groups using countGroups function
- **Line 40**: Allocate array to store word counts for each group
- **Line 42**: Allocate temporary array for group strings
- **Line 43**: Initialize group index
- **Line 44**: Initialize current group accumulator
- **Line 45-56**: Parse groups separated by '#' delimiter
- **Line 46-50**: Handle group delimiter - store current group
- **Line 47**: Store accumulated group in array
- **Line 48**: Reset current accumulator
- **Line 52-55**: Handle regular characters - accumulate
- **Line 57**: Store the last group after loop
- **Line 59**: Allocate main 2D array of string pointers
- **Line 60-84**: Process each group to extract words
- **Line 61**: Count words in current group
- **Line 62**: Allocate row for exact number of words
- **Line 64**: Initialize word index for current group
- **Line 65**: Initialize word accumulator
- **Line 66**: Loop through characters in group
- **Line 67-74**: Handle space delimiter between words
- **Line 68-72**: Store completed word if not empty
- **Line 75-77**: Handle regular characters - accumulate
- **Line 78-81**: Store the last word after loop
- **Line 83**: Clean up temporary groups array
- **Line 85**: Return the created dynamic array
- **Purpose**: Converts thesaurus string to organized 2D array structure

### Main Function (Lines 92-138)
```cpp
int main()
{
    char ws[] = "abandon discontinue vacate#absent missing unavailable#cable wire#calculate compute determine measure#safety security refuge";

    int numGroups = 0;
    int *rowSizes = nullptr;
    string **thesaurus = CreateDynamicArray(ws, numGroups, rowSizes);

    cout << "===== Thesaurus Array =====" << endl;
    for (int i = 0; i < numGroups; i++)
    {
        cout << "Group " << i << ": ";
        for (int j = 0; j < rowSizes[i]; j++)
        {
            cout << "[" << thesaurus[i][j] << "] ";
        }
        cout << endl;
    }

    cout << "\nEnter word to paraphrase: ";
    string userWord;
    cin >> userWord;

    bool found = false;
    for (int i = 0; i < numGroups; i++)
    {
        if (rowSizes[i] > 0 && thesaurus[i][0] == userWord)
        {
            cout << "Replaced word: " << thesaurus[i][rowSizes[i] - 1] << endl;
            found = true;
            break;
        }
    }
    if (!found)
    {
        cout << "Word not found in thesaurus." << endl;
    }

    for (int i = 0; i < numGroups; i++)
    {
        delete[] thesaurus[i];
    }
    delete[] thesaurus;
    delete[] rowSizes;

    return 0;
}
```
- **Line 92**: Main function entry point
- **Line 94**: Hard-coded thesaurus string with '#' group separators
- **Line 96**: Initialize number of groups counter
- **Line 97**: Initialize row sizes pointer
- **Line 98**: Create dynamic thesaurus array from string
- **Line 100**: Display thesaurus array header
- **Line 101-108**: Display all synonym groups
- **Line 102**: Display group number
- **Line 104**: Loop through words in current group
- **Line 105**: Display each word in brackets
- **Line 111**: Prompt user for word to paraphrase
- **Line 112**: Get user input word
- **Line 114**: Initialize found flag
- **Line 115-124**: Search for user word in thesaurus
- **Line 116**: Check if group has words and first word matches
- **Line 117**: Display replacement word (last word in group)
- **Line 118**: Set found flag to true
- **Line 119**: Exit search loop
- **Line 121-125**: Handle word not found case
- **Line 127-131**: Memory cleanup - delete all allocated arrays
- **Line 133**: Return success

## Design Patterns and Concepts Used

### 1. **String Parsing and Tokenization**
- Custom delimiter-based parsing using '#' for groups
- Space-based word separation within groups
- Manual string manipulation without built-in split functions

### 2. **Dynamic Memory Management**
- 2D dynamic arrays with `new` and `delete[]`
- Reference parameters for output values (`int &`, `int *&`)
- Proper cleanup order to prevent memory leaks

### 3. **Data Structure Transformation**
- Convert flat string to structured 2D array
- Organize synonyms into logical groups
- Efficient storage with jagged arrays

### 4. **Search and Replacement Logic**
- Linear search through synonym groups
- First-word matching strategy
- Last-word replacement for paraphrasing

### 5. **User Interaction**
- Clear thesaurus display format
- Simple word input interface
- Informative feedback for found/not found cases

### 6. **Modular Design**
- Separate functions for counting, parsing, and array creation
- Reusable utility functions
- Header file organization for clean interfaces

### 7. **C-Style String Handling**
- Null-terminated string processing
- Character-by-character iteration
- Compatibility with both C and C++ string operations

This implementation demonstrates comprehensive C++ programming skills including string parsing, dynamic memory management, data structure transformation, and modular design principles for a practical thesaurus-based writing assistant.
