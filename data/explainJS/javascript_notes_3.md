# Functions in JavaScript

Functions are reusable blocks of code.

## Syntax

``` javascript
function greet(name) {
  return "Hello " + name;
}
```

## Arrow Functions

``` javascript
const greet = (name) => "Hello " + name;
```
